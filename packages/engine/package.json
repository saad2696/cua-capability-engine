{
  "name": "@cua/engine",
  "version": "0.1.0",
  "private": true,
  "description": "Surface abstraction, discovery loop, recorder, deterministic replay, policy, session control, escalation, evidence.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": { "@cua/schema": "workspace:*" },
  "devDependencies": { "@cua/config": "workspace:*" }
}
