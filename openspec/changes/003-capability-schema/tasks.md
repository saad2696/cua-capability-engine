# Tasks
- [x] 3.1 Zod schemas: Locator, Value, Assertion, Wait, Step, Outcome, Policy, Capability
- [x] 3.2 Zod schemas: ReplayResult, DiscoveryResult, Event
- [x] 3.3 Cross-field validation: outputs.from references an existing extract step; param refs exist in inputs
- [x] 3.4 Example artifact `artifacts/examples/member-savings-balance@1.json` that validates
- [x] 3.5 `docs/artifact-schema.md` with the annotated example and rationale per field
- [x] 3.6 Unit tests: valid example passes; missing checkpoint fails; literal secret fails
- [x] 3.7 `cua artifact validate <file>` command
- [x] 3.8 ScreenSignature type + landmark matching rule
- [x] 3.9 Step flags: retryable, pointOfNoReturn, optional, onFailure, timeoutMs
- [x] 3.10 Candidate confidence/stats; extraction contract with candidates/parse/validate
- [x] 3.11 `requires`, `summary`, `changelog`; migration scaffold with test
- [x] 3.12 Schema docs updated with every new field and its rationale
