# Tasks
- [x] 1.1 `pnpm init`, `pnpm-workspace.yaml`, root `package.json` scripts
- [x] 1.2 `packages/config` with `tsconfig.base.json` and eslint flat config
- [x] 1.3 Create the five packages with `package.json`, `tsconfig.json`, `src/index.ts`
- [x] 1.4 `.gitignore`, `.env.example`, `.editorconfig`
- [x] 1.5 Vitest root config + one smoke test per package
- [x] 1.6 README skeleton: Overview, Setup, Demo path, Architecture, Repository layout, Evidence, Docs
- [x] 1.7 `pnpm install && pnpm -r build && pnpm test` green; first commit
